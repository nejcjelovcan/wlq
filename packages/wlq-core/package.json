{
  "name": "@wlq/wlq-core",
  "version": "1.0.0",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "license": "MIT",
  "scripts": {
    "lint": "eslint src --ext ts,tsx",
    "lint:fix": "yarn lint --fix",
    "verify:lint": "yarn lint --max-warnings=0",
    "verify:tsc": "tsc --noEmit",
    "verify": "yarn verify:lint && yarn verify:tsc",
    "test": "dotenv -e .env.test jest",
    "test:coverage": "dotenv -e .env.test yarn jest -- --coverage --silent",
    "test:watch": "dotenv -e .env.test yarn jest -- --watch-all --silent",
    "build:tsc": "tsc",
    "build:watch": "tsc -w",
    "build": "rm -rf lib/ && yarn build:tsc"
  },
  "dependencies": {
    "fp-ts": "^2.8.4",
    "io-ts": "^2.2.11",
    "jose": "^2.0.2",
    "nanoid": "^3.1.15"
  },
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "dotenv-cli": "^4.0.0",
    "jest": "^26.6.1",
    "ts-jest": "^26.4.1"
  }
}
